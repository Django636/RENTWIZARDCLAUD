{
  "name": "@rentwizard/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "prisma generate",
    "typecheck": "echo 'prisma schema validated via prisma validate'",
    "lint": "echo 'no linter'",
    "test": "echo 'no tests'",
    "validate": "prisma validate",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "prisma db seed",
    "studio": "prisma studio"
  },
  "prisma": {
    "schema": "./schema.prisma",
    "seed": "npx ts-node --compiler-options {\"module\":\"CommonJS\"} seed/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "bcrypt": "^5.1.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.6.0",
    "@types/bcrypt": "^5.0.0"
  }
}
